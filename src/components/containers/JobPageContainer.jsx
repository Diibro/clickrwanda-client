import PropTypes from "prop-types";

const JobPageContainer = ({job}) => {
     return (
          <div className="job-page-contaiener">
               
          </div>
     )
}

JobPageContainer.propTypes = {
     job: PropTypes.object
}
export default JobPageContainer